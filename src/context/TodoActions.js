import { ACTIONS } from "./TodoReducer"

const LOCAL_STORAGE_KEY = "todoApp.todos"
const LOCAL_STORAGE_KEY_CAT_ID = "todoApp.categoryId"
const LOCAL_STORAGE_KEY_CATS = "todoApp.categories"

export const addCategory = (getName) => {
    return getName
    
  }